# Must precede `use flake` so nix-direnv's cache check sees these watches.
watch_dir flake

use flake

watch_file pyproject.toml
watch_file uv.lock

# Setup Python dependencies
uv sync --extra test --quiet || log_error "uv sync failed"
PATH_add "$PWD/.venv/bin"
